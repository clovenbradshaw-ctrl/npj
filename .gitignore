# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# secrets / local config — never commit tokens
.env
.env.*
*.local

# scratch + generated
screenshots/
uploads/
scraps/
*.log

# dev-only deps (the app is no-build; Babel/React load from unpkg at runtime)
node_modules/

# editing-only post structure — never published, never built (spec §9, Invariant I1)
.structure/
