{
  "name": "npj",
  "version": "0.0.0",
  "private": true,
  "description": "People's Journalism — a community newsroom prototype. The app is no-build (in-browser Babel); this manifest exists only for the structure-layer test suite + CI.",
  "scripts": {
    "test": "node --test tests/*.test.js && node tests/leakage-gate.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.61.1"
  }
}
